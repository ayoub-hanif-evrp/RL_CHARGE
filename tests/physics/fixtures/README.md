# Energy reference fixtures

`energy_reference.json` is produced by `write_energy_reference.py`.

The numbers come from `src/physics/official_replica.py`, a transcription of the
EVRPTW-GR Model 2 linearized energy block (no Gurobi). They are not copied from
published result tables.
