# Legacy/internal configuration — not used in paper experiments.
# One optimizer; frozen target. Historical two-stage Double DQN on the
# new simulator. Do not train, evaluate, or tabulate this method for the paper.

gradient_steps = 102400
batch_size = 32
target_sync = 500
replay_size = 20000
learning_rate = 1.0e-3
d_model = 64
gamma = 1.0
seed = 42
val_interval = 2560
early_stopping_patience = 20
